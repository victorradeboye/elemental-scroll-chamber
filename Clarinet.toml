[project]
name = "elemental-scroll-chamber"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/elemental-scroll-chamber/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.elemental-scroll-chamber]
path = "contracts/elemental-scroll-chamber.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
